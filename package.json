{
  "name": "tsforge-github-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Action: run tsforge review against a PR and post findings as inline, resolvable review comments.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/boringstack-xyz/tsforge-github-review.git"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0",
    "bun-types": "^1.4.0"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
